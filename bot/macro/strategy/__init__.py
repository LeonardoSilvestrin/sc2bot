"""What this game converges toward -- declared here, decided elsewhere.

`goals.py` is the vocabulary of a goal set, `profiles.py` one goal set per
strategy, `openings.py` which one follows which opening, `config.py` the
costs and posture-adjusted priorities a planner prices them with, and
`reference_build.py` a sourced timing floor for production structures.
Nothing here reads Attention: the spend domains beside this folder do.
"""
