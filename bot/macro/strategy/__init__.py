"""What this game converges toward -- declared here, decided elsewhere.

`goals.py` is the vocabulary of a goal set, `openings.py` maps Ares opening
names to the vertical plans under ``bot.macro.builds``, `config.py` holds the
shared pricing knobs, and `reference_build.py` is a sourced timing floor for
production structures. `profiles.py` remains only as a compatibility import.
Nothing here reads Attention: the spend domains beside this folder do.
"""
