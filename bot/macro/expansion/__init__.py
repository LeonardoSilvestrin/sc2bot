"""Bases to take: `EXPAND`.

`bases.py` decides *when* the current bases are saturated enough for the
posture. *Where* is Ares' `ExpansionController`'s answer, and whether a base
beats a Tank or an upgrade this tick is priority and bank, arbitrated by
`EconomyController`.
"""
