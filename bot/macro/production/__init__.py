"""Units to train: `PRODUCE_UNIT` and `PRODUCE_WORKER`.

`army_demand.py` is the assessment -- how far the army is below the
strategy's supply target, and which composition members are owed -- and
`army.py` and `workers.py` each argue for one more. An operational wish from
a behavior (two Banshees for a raid, say) belongs as an input to that demand,
never as a purchase of its own.
"""
