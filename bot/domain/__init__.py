"""Temporary home of the legacy ``MacroPosture`` enum (``posture.py``).

The capability model that used to live here -- unit profiles, suitability,
roles -- is gone: each behavior names the concrete unit types it can use.
This package disappears once macro policy stops travelling through Awareness.
"""
